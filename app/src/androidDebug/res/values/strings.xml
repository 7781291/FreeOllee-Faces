<resources>
    <!-- Debug builds install as a separate app (….debug); the label leads with "Debug" so it
         stays distinguishable from the release "Super FreeOllee" even when the launcher truncates
         long titles (a trailing "(debug)" gets cut off and the two look identical otherwise). -->
    <string name="app_name">Debug FreeOllee</string>
</resources>
