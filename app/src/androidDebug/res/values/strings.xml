<resources>
    <!-- Debug builds install as a separate app (….debug); a distinct label makes it obvious
         in the drawer which one is the dev/test build vs the real GitHub release. -->
    <string name="app_name">FreeOllee Debug</string>
</resources>
